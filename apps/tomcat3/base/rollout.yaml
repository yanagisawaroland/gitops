apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: tomcat3
  namespace: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tomcat3
  template:
    metadata:
      labels:
        app: tomcat3
    spec:
      containers:
        - name: tomcat3
          image: harbor.devops.local:7581/devops/tomcat3:bbf2b702
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
            - name: jmx
              containerPort: 9404
          env:
            - name: APP_VERSION
              value: "1.0.0"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 256Mi
              cpu: 500m
  strategy:
    canary:
      stableService: tomcat3-stable
      canaryService: tomcat3-canary
      trafficRouting:
        nginx:
          stableIngress: tomcat3
      analysis:
        templates:
          - templateName: success-rate
        args:
          - name: ingress-name
            value: tomcat3
      steps:
        - setWeight: 20
        - pause: {duration: 30s}
        - setWeight: 50
        - pause: {duration: 30s}
        - setWeight: 80
        - pause: {duration: 30s}
