apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: testapp
  namespace: monitoring
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
      - production
  selector:
    matchLabels:
      app: testapp
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
