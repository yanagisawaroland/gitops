apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: success-rate
  namespace: production
spec:
  args:
    - name: ingress-name
  metrics:
    - name: success-rate
      interval: 30s
      successCondition: result[0] >= 0.95
      failureLimit: 3
      provider:
        prometheus:
          address: http://prometheus-kube-prometheus-prometheus.monitoring.svc:9090
          query: |
            sum(rate(nginx_ingress_controller_requests{ingress="{{args.ingress-name}}",status!~"5.."}[2m]))
            /
            sum(rate(nginx_ingress_controller_requests{ingress="{{args.ingress-name}}"}[2m]))
